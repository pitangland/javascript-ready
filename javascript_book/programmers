// 문자열 계산하기
const quiz = ["3 - 4 = -3", "5 + 6 = 11"];
//	return ["X", "O"]

solution(quiz);

// eval 함수를 되도록이면 사용하지 말래..
function solution(zuia) {
  let answer = [];

  answer = my_string.split(" ");
  console.log(answer);

  return answer;
}
